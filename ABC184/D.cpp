#include<bits/stdc++.h>
using namespace std;

vector<int> f[100][100][100];

int main() {
    int A, B, C;
    cin >> A >> B >> C;

    int draw = 1;

    for (draw; draw < 100; draw++) {
        if (A + draw < 100) {
            f[A + draw][B][C] = 
        }
    }


}